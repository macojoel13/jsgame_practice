function start() {
    var canvas = document.getElementById('canvas');
    var context = document.getContext('2d');
    canvas.width = 500;
    canvas.height = 500;
    context.fillStyle='black';
    context.fill();
    
    
}